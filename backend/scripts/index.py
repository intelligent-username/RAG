"""
Place chunked sets into a vector database
Uses sine-cosine similarity
Stores the database locally
"""

def index(chunks):
    """
    Takes in a list of chunks and indexes them into a vector database.
    The embedding algorithm used is sine-cosine similarity.

    Expected chunk format is defined in chunk.py
    """
    ...
